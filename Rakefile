#!/usr/bin/env rake
# Add your own events in files placed in lib/events ending in .rake,
# for example lib/events/capistrano.rake, and they will automatically be available to Rake.

require File.expand_path('../config/application', __FILE__)

Agenda::Application.load_tasks
